# DevPulse Frontend

React + Vite + Tailwind + SCSS dashboard for DevPulse.

## Setup
1. Copy `.env.example` to `.env` and fill in real values
2. `npm install`
3. `npm run dev`

## Env Vars
- VITE_CLERK_PUBLISHABLE_KEY - from Clerk dashboard
- VITE_API_BASE_URL - backend API URL
